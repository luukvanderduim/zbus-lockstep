# git-cliff ~ default configuration file
# https://git-cliff.org/docs/configuration
#
# Lines starting with "#" are comments.
# Configuration options are organized into tables and keys.
# See documentation for more information on available options.

[changelog]
# changelog header
header = """
# zbus-lockstep Changelog\n
All notable changes to this project will be documented in this file.\n
"""
# template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
            {% if commit.breaking %}[**breaking**] {% endif %}\
            {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
# template for the changelog footer
footer = """
<!-- generated by git-cliff -->
"""
# remove the leading and trailing s
trim = true
# postprocessors
postprocessors = [
  { pattern = 'git@github.com:luukvanderduim/zbus-lockstep.git', replace = "https://github.com/luukvanderduim/zbus-lockstep" }, # replace repository URL
]

[git]
# parse the commits based on https://www.conventionalcommits.org
conventional_commits = false
# filter out the commits that are not conventional
filter_unconventional = false
# process each line of a commit as an individual commit
split_commits = false
# regex for preprocessing the commit messages
commit_preprocessors = [
  # Replace issue numbers
  #{ pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](<REPO>/issues/${2}))"},
  # Check spelling of the commit with https://github.com/crate-ci/typos
  # If the spelling is incorrect, it will be automatically fixed.
  #{ pattern = '.*', replace_command = 'typos --write-changes -' },
]
# regex for parsing and grouping commits
commit_parsers = [
  { message = "^🎉", group = "<!== 0 -->🎉 Begin project" },
  { message = "^✨", group = "<!-- 1 -->✨ Features" },
  { message = "^💥", group = "<!== 2 -->💥 Breaking change" },
  { message = "^🐛|🚑️|🩹", group = "<!-- 3 -->🐛 Fixes" },
  { message = "^📝", group = "<!-- 4 -->📝 Documentation" },
  { message = "^⚡", group = "<!-- 5 -->⚡ Performance" },
  { message = "^♻️|🚚", group = "<!-- 6 -->♻️ Refactor / Move files" },
  { message = "^💄|🎨", group = "<!-- 7 -->💄Art and Style" },
  { message = "^✅|🧪|🦺", group = "<!-- 8 -->✅ Testing and validation" },
  { message = "^chore\\(release\\): prepare for", skip = true },
  { message = "^chore\\(deps.*\\)", skip = true },
  { message = "^chore\\(pr\\)", skip = true },
  { message = "^chore\\(pull\\)", skip = true },
  { message = "^chore|ci", group = "<!-- 9 -->⚙️ Miscellaneous Tasks" },
  { body = ".*🔒️|🔐", group = "<!-- 10 -->🔒️ Security / Secrets" },
  { message = "^⏪️|🔥", group = "<!-- 11 -->⏪️ Revert / Remove" },
  { message = "^👽️", group = "<!== 13 -->👽️ External API changes" },
  { message = "^💥", group = "<!== 14 -->💥 Breaking change" },
  { message = "^♿️", group = "<!== 15 -->♿️ Accessibility" },
  { message = "^🚸", group = "<!== 16 -->🚸 Improve UX" },
  { message = "^🧑‍💻", group = "<!== 17 -->🧑‍💻 Improve DX" },
  { message = "^🚩", group = "<!== 18 -->🚩 Feature flags" },
  { message = "^🧵", group = "<!== 19 -->🧵 Multithreading / concurrency" },
]
# protect breaking changes from being skipped due to matching a skipping commit_parser
protect_breaking_commits = false
# filter out the commits that are not matched by commit parsers
filter_commits = false
# regex for matching git tags
# tag_pattern = "v[0-9].*"
# regex for skipping tags
# skip_tags = ""
# regex for ignoring tags
# ignore_tags = ""
# sort the tags topologically
topo_order = false
# sort the commits inside sections by oldest/newest order
sort_commits = "oldest"
# limit the number of commits included in the changelog.
# limit_commits = 42
